# معماری موتور متن‌به‌گفتار آوا (AvaCore TTS Architecture)

این سند جزئیات فنی و معماری **فعلی** پروژهٔ **AvaCore** را تشریح می‌کند. این موتور ۱۰۰٪ آفلاین است و در زمان اجرا هیچ وابستگی به اینترنت ندارد.

## ۱. پشتهٔ فناوری (Technology Stack)

| لایه | فناوری | توضیح |
| :--- | :--- | :--- |
| موتور استنتاج | **Sherpa-ONNX 1.10.41** (`app/libs/sherpa-onnx.aar`) | پوشش JNI + کاتلین روی ONNX Runtime؛ ابتدا شتاب‌دهندهٔ `nnapi` را امتحان می‌کند، در صورت شکست به `cpu` برمی‌گردد (نکته زیر) |
| مدل صوتی + وکودر | **Piper VITS**، یک مدل به‌ازای هر زبان (`model_<lang>.onnx`) | مدل سرتاسری ۲۲۰۵۰ هرتز. امروز: فارسی، انگلیسی، سوئدی — فقط مدل فارسی داخل APK بسته‌بندی می‌شود |
| تأمین صدا | `service/ModelDownloader.kt` | انگلیسی/سوئدی بسته‌بندی نمی‌شوند — با اولین درخواست واقعی، روی خود گوشی دانلود و استخراج (tar+bzip2 با `commons-compress`) و برای همیشه کش می‌شوند |
| تبدیل نویسه به واج (G2P) | **eSpeak-NG** (`espeak-ng-data/`، مشترک بین زبان‌ها) | واج‌سازی فارسی سخت‌ترین بخش است؛ انگلیسی/سوئدی از پشتیبانی خوب خودِ eSpeak استفاده می‌کنند |
| پیش‌پردازش متن | پکیج `nlp/` | **فقط فارسی**: یکسان‌سازی، بسط اعداد/تاریخ/ارز/مخفف‌ها، واژه‌نامه، قطعه‌بندی، SSML. زبان‌های دیگر فقط SSML + قطعه‌بندی می‌گیرند. |
| یکپارچگی سیستم | `AvaTtsService : TextToSpeechService` | هر زبان بسته‌بندی‌شده را از طریق `onGetVoices`/`onIsLanguageAvailable`/`onIsValidVoiceName`/`onLoadVoice` اعلام می‌کند؛ مدل هر زبان با اولین درخواست، با تأخیر (lazy) بارگذاری می‌شود |

> نکته: VITS یک شبکهٔ سرتاسری است؛ وکودر (دیکودر شبیه HiFi-GAN) **داخل خود مدل** قرار دارد. تاکوترون یا WaveRNN جداگانه‌ای وجود ندارد.

> نکتهٔ NNAPI: موتور ابتدا شتاب‌دهندهٔ NNAPI اندروید را امتحان می‌کند، اما این تضمینی برای سریع‌تر شدن واقعی نیست — روی سخت‌افزاری که این تغییر رویش تست شد، لایهٔ نیتیو خودِ این نسخهٔ Sherpa-ONNX بر اساس سطح API گیت می‌شود و صرف‌نظر از دستگاه، همیشه به CPU برمی‌گردد (بدون کرش). این را «امتحان‌شده و بی‌خطر» بدانید، نه «شتاب واقعی تأییدشده».

## ۲. تأمین دارایی‌ها (Assets)

دارایی‌های بزرگ توسط اسکریپت `download_assets.sh` تأمین می‌شوند (دانلود فقط در زمان توسعه؛ اجرای روی گوشی آفلاین است). اسکریپت **فقط مدل فارسی** را دانلود می‌کند — تنها زبانی که داخل APK بسته‌بندی می‌شود. انگلیسی و سوئدی توسط این اسکریپت دانلود **نمی‌شوند**؛ `ModelDownloader` هرکدام را با اولین درخواست واقعی از همان بسته‌های انتشار `k2-fsa/sherpa-onnx` روی خود گوشی می‌گیرد (نیازمند مجوز `INTERNET`، از پیش در مانیفست اعلام شده):

| فایل | مسیر | توضیح |
| :--- | :--- | :--- |
| Sherpa-ONNX | `app/libs/sherpa-onnx.aar` | شامل کتابخانه‌های نیتیو |
| Neural Model (فارسی، بسته‌بندی‌شده) | `app/src/main/assets/tts/model_fa.onnx` | VITS با متادیتای `sample_rate=22050` |
| Neural Model (انگلیسی/سوئدی، دانلود آنی) | `filesDir/model_en.onnx`, `filesDir/model_sv.onnx` | با اولین درخواست هر زبان توسط `ModelDownloader` دانلود و در حافظهٔ خصوصی برنامه کش می‌شوند |
| Phoneme Tokens | `…/tokens_<lang>.txt` | توکن‌های واج، یکی به‌ازای هر زبان |
| eSpeak-NG Data | `…/espeak-ng-data/` | پایگاه دادهٔ G2P، مشترک بین همهٔ زبان‌ها |
| Pronunciation Lexicon | `…/lexicon.txt` | واژه‌نامهٔ تلفظ/اضافهٔ دستی — فقط برای فارسی استفاده می‌شود؛ شامل عبارات نهادی رایج و واژگان حوزهٔ آموزش زبان |

## ۳. خط لولهٔ پردازش متن فارسی (`nlp/`)

متن خام پیش از رسیدن به مدل، از `TextProcessor` عبور می‌کند (ترتیب مهم است):

1. **`Ssml`** — پشتیبانی از SSML: `<break>`، `<say-as>`، و اکنون `<prosody rate/pitch>` و `<emphasis level>` که به‌صورت ضریب واقعی سرعت/زیروبمی روی هر قطعه اعمال می‌شوند؛ متن ساده دست‌نخورده عبور می‌کند. `<phoneme>` پشتیبانی نمی‌شود — پوششِ Piper هیچ راهی برای بازنویسی تلفظ تک‌واژه ندارد.
2. **`AbbreviationExpander`** — جدول کوتاه و محتاطانهٔ مخفف‌های تمام‌واژه (نشانه‌های تقویمی، واحدهای متریک) که eSpeak خوانش درستی برایشان ندارد.
3. **`DateCurrencyExpander`** — تاریخ‌های شمسی نوشته‌شده به‌صورت گروه رقمی (`۱۴۰۴/۳/۱۲` ← «دوازدهم خرداد هزار و چهارصد و چهار») و نمادهای ارزی (`$€£﷼₹`) چسبیده به عدد.
4. **`NumberToWords`** — بسط کامل اعداد فارسی: اصلی، ترتیبی، اعشاری، درصد (ارقام فارسی/عربی/لاتین). پیش از یکسان‌سازی نشانه‌ها اجرا می‌شود تا جداکنندهٔ هزارگان حفظ شود.
5. **`Normalizer`** — تبدیل نویسه‌های عربی به فارسی، یکسان‌سازی نیم‌فاصله (ZWNJ)، حذف کشیده/تنوین، فاصله‌گذاری نشانه‌ها.
6. **`PronunciationLexicon`** — اصلاح تلفظ و کسرهٔ اضافه از روی `lexicon.txt` (محتاطانه و قابل گسترش).
7. **`SentenceSegmenter`** — تقسیم به واحدهای کوتاه با مکث‌های عروضی، با انتقال ضریب سرعت/زیروبمی هر قطعه.

## ۴. سرویس و پخش (`AvaTtsService`)

- **مقداردهی Lazy**: مدل در تردی پس‌زمینه بارگذاری می‌شود و `CountDownLatch` آماده‌بودن را اعلام می‌کند (بدون انتظار فعال/busy-wait).
- **مهاجرت دارایی‌ها**: نسخه‌دار (`ASSETS_VERSION`) و اتمیک (کپی به فایل موقت سپس تغییر نام). کپی ناقص یا نسخهٔ قدیمی به‌صورت خودکار ترمیم می‌شود.
- **پخش جریانی (Streaming)**: با `generateWithCallback`، صوت جمله‌به‌جمله و به‌محض تولید پخش می‌شود؛ تأخیر تا اولین صوت مستقل از طول متن، تقریباً ثابت می‌ماند.
- **توقف فوری**: `onStop` وسط گفتار اعمال می‌شود (callback مقدار توقف برمی‌گرداند).
- **سرعت گفتار**: `speechRate` سیستم به ضریب `speed` موتور نگاشت می‌شود، و برای هر قطعه با ضریب SSML `<prosody>`/`<emphasis>` (در صورت وجود) ضرب می‌شود.
- **زیروبمی (Pitch)**: با تغییر `pitch` سیستم، پس‌پردازش **SOLA** اعمال می‌شود (`dsp/PitchShifter` — کشش زمانی + بازنمونه‌برداری با حفظ مدت)، به همان ترتیب برای هر قطعه. در حالت پیش‌فرض صوت کاملاً دست‌نخورده می‌ماند.
- **تکه‌بندی صوت**: تکه‌های حداکثر **۸ کیلوبایت** برای سازگاری با OEMهای سخت‌گیر (Oppo/OnePlus).
- **صفحهٔ آزمایش صدا**: `MainActivity` برای هر صدای نصب‌شده یک دکمه دارد که مسیر واقعی `setVoice()` → `speak()` را با متن نمونهٔ همان زبان اجرا می‌کند و نتیجه را روی صفحه نشان می‌دهد.

## ۵. نکات حیاتی بیلد و استقرار

### لامبدای کلاس‌محور (حیاتی)
در `app/build.gradle.kts` پرچم‌های `-Xlambdas=class` و `-Xsam-conversions=class` تنظیم شده‌اند. کاتلین ۲.۰ لامبداها را به‌صورت `invokedynamic` کامپایل می‌کند؛ در این حالت لامبدای کال‌بک فاقد متد تخصصی `invoke([F)Integer` است که کد نیتیو `generateWithCallback` با JNI به‌دنبال آن می‌گردد و کل پروسه با SIGABRT کرش می‌کند. **این پرچم‌ها نباید حذف شوند.**

### سازگاری با اندروید ۱۵ (16KB Page Size)
`useLegacyPackaging = true` در بسته‌بندی، تا کتابخانه‌های نیتیو در زمان نصب استخراج و با Page Size دستگاه هم‌تراز شوند.

### معماری‌ها و کتابخانه‌های نیتیو
فقط `arm64-v8a` و `armeabi-v7a` بسته‌بندی می‌شوند. کتابخانه‌های نیتیو از داخل **AAR** تأمین می‌شوند؛ پوشه‌های `app/libs/jniLibs` و `app/libs/tmp_extract` (که استفاده نمی‌شدند) حذف شده‌اند.

### مجوزهای خاص (Oplus/Background)
روی دستگاه‌های چینی کاربر باید «Allow background activity» را دستی فعال کند:
`Settings > Battery > App Battery Management > AvaCore`

## ۶. یکپارچگی با سیستم

- `onIsLanguageAvailable` هر زبان ثبت‌شده در `VoiceRegistry.VOICES` را می‌پذیرد (کد ۲ حرفی یا ISO-3 — چون نقاط تماس مختلف فریم‌ورک هر دو را می‌فرستند). منطق تطبیق در خودِ `VoiceRegistry` (کاتلین خالص، بدون وابستگی به اندروید) است تا بدون شبیه‌ساز قابل تست باشد.
- **`TtsDataCheckActivity`**:
  - `CHECK_TTS_DATA`: سه لوکیل ISO-3 («`fas-IRN`», «`eng-USA`», «`swe-SWE`») اعلام می‌کند تا در تنظیمات TTS هر سه زبان دیده شوند.
  - `GET_SAMPLE_TEXT`: بر اساس زبان درخواستی، متن نمونهٔ همان زبان را با کد نتیجهٔ **`LANG_AVAILABLE`** برمی‌گرداند (نه `RESULT_OK`؛ در غیر این صورت صفحهٔ تنظیمات به متن انگلیسی پیش‌فرض برمی‌گردد که با صدای اشتباه نامفهوم خوانده می‌شود).

## ۷. نقشهٔ راه

جزئیات کامل و وضعیت هر مورد در **`PLAN.md`** نگهداری می‌شود؛ این فقط خلاصه است.

### انجام‌شده
- **دانلود آنی صدا**: فقط فارسی داخل APK است؛ انگلیسی/سوئدی با اولین درخواست دانلود و کش می‌شوند (`ModelDownloader`).
- **آزمایش NNAPI**: امتحان شد، بی‌خطر بودنش تأیید شد (به CPU برمی‌گردد)؛ روی سخت‌افزار تست‌شده شتاب واقعی مشاهده نشد.
- **صفحهٔ آزمایش صدا در برنامه**: یک دکمه به‌ازای هر صدای نصب‌شده.
- **غنی‌سازی نرمال‌ساز**: بسط تاریخ شمسی و نمادهای ارزی، به‌همراه جدول کوتاه و محتاطانهٔ مخفف‌ها.
- **`<prosody>`/`<emphasis>` در SSML**: کنترل واقعی سرعت/زیروبمی به‌ازای هر قطعه، به‌صورت سرتاسر تأیید شده.

### هنوز باز
- **پیش‌بینی اضافه و رفع ابهام هم‌نگاشت‌ها**: با مدل یادگیری ماشین (جایگزین واژه‌نامهٔ دستی فعلی) — همان چیزی که واقعاً شکاف باقی‌مانده تا فارسی طبیعی را پر می‌کند؛ یک پروژهٔ واقعی یادگیری ماشین (داده، آموزش، ارزیابی)، نه یک افزودهٔ سریع.
- **`<phoneme>` در SSML**: پوششِ فعلی Piper راهی برای بازنویسی تلفظ تک‌واژه ندارد؛ نیازمند تغییر در لایهٔ موتور است.
- **کوانتیزه‌سازی INT8**: آزمایش و کنار گذاشته شد — با این نسخهٔ Sherpa هنگام بارگذاری مدل کرش می‌کند و به‌دلیل فشرده‌سازی زیپ، حجم APK را هم کاهش نمی‌دهد. برای کاهش حجم، تفکیک per-ABI (App Bundle) مؤثرتر است.
- **مدل‌های جایگزین**: Matcha-TTS / Kokoro (کلاس‌های پیکربندی در Sherpa موجودند).

---
**توسعه‌دهنده:** AliRezaTaleghani
**تاریخ آخرین ویرایش:** سپتامبر ۲۰۲۶
